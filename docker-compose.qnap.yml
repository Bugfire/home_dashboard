version: '2'

services:
  app:
    image: bugfire/home_dashboard:0.1.5
    volumes:
      - "/share/Docker/Volumes/home_dashboard/config:/config:ro"
    restart: always
    cpu_shares: 20
    mem_limit: "2048m"
    ports:
      - "3000:3000"
    logging:
      driver: syslog
      options:
        syslog-address: "udp://192.168.13.11:514"
        syslog-format: "rfc3164"
        tag: "home_dashboard"
